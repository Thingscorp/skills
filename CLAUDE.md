# CLAUDE.md

Read `AGENTS.md` first. Then load one skill from `skills/` that matches the job.

Install for this machine: `npx skills add Thingscorp/skills` or `./tools/install.sh --dest ~/.claude/skills`.
