# Quality artifacts

The living feature sheet does **not** live here. `quality-loop` keeps it
outside the product (sibling workspace `skills-qa/features.csv`).

This library's last executed pass is the conversation + git history, not a
checked-in spreadsheet.
